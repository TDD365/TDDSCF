#ifndef _CTRL_HPP
#define _CTRL_HPP

#include <iostream>
#include <cstring>
#include <fstream>
#include <iomanip>
using namespace std;

// Options
inline int prtlevel = 0;

// Read
void readControls(const std::string& filename);

#endif // _CTRL_HPP